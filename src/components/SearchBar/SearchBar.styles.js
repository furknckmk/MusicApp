/* eslint-disable prettier/prettier */
import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        backgroundColor: '#eceff1',
        borderColor: '#ffcccc',
        padding:1,margin:5,borderRadius:10,borderWidth:1,
    },
});
